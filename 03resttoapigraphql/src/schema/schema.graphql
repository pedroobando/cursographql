type Query {
  seasonsList: [Season!]!
}

type Season {
  year: String!
  url: String!
  urlMobile: String!
}

type Location {
  lat: String!
  lng: String!
  locality: String!
  country: String!
}

type Circuit {
  id: ID!
  url: String!
  name: String!
  location: Location!
}

type Race {
  season: String!
  round: String!
  url: String!
  name: String!
  circuit: Circuit!
  date: String!
  time: String!
}

type Mutation {
  add(value: String!): [String]
}
